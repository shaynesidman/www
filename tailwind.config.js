module.exports = {
    purge: [],
    theme: {
        extend: {
            fontFamily: {
                primary: ['var(--font-primary)'],
                secondary: ['var(--font-secondary)'],
                mono: ['var(--font-mono)'],
            },
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
};